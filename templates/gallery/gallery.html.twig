{% extends 'base.html.twig' %}

{% block title %}Galerie - 3wolfdesign{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('styles/product.css') }}">
{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="text-center mb-5 page-title">
        <h1 class="page-title fw-bold">Impression 3D</h1>
        <p class="lead">Découvrez nos dernières réalisations d'impression.</p>
        <hr class="w-25 mx-auto text-primary" style="height: 3px;">
    </div>

    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-4 g-4">
        {% for produit in realisations %}
            <div class="col">
                <div class="card h-100 shadow-sm border-0 gallery-card position-relative">
                    <div class="overflow-hidden rounded-top bg-light" style="height: 250px;">
                        {% if produit.img_path %}
                            <img src="{{ asset('img/produits/' ~ produit.img_path) }}" 
                                class="card-img-top" 
                                alt="{{ produit.nom }}"
                                style="object-fit: fill; height: 100%;">
                        {% else %}
                            <div class="d-flex align-items-center justify-content-center h-100">
                                <i class="fas fa-image fa-3x text-muted"></i>
                            </div>
                        {% endif %}
                    </div>
                    
                    <div class="card-body">
                        <h5 class="card-title fw-bold text-dark mb-1">
                            <a href="{{ path('app_gallery_show', {id: produit.id}) }}" class="text-decoration-none text-dark stretched-link">
                                {{ produit.nom }}
                            </a>
                        </h5>
                        <p class="card-text text-muted small">{{ produit.description|slice(0, 60) }}</p>
                    </div>
                </div>
            </div>
        {% endfor %}

        {# Affichée uniquement pour l'admin #}
        {% if is_granted('ROLE_ADMIN') %}
            <div class="col">
                <div class="card h-100 border-dashed shadow-sm bg-light gallery-card d-flex align-items-center justify-content-center text-center" style="border: 2px dashed #dee2e6; min-height: 300px;">
                    <a href="{{ path('app_admin_product_new') }}" class="stretched-link text-decoration-none text-muted">
                        <div class="card-body d-flex flex-column justify-content-center">
                            <i class="fas fa-plus-circle fa-4x mb-3 text-primary"></i>
                            <h5 class="fw-bold">Ajouter une réalisation</h5>
                            <p class="small">Nouvelle impression 3D</p>
                        </div>
                    </a>
                </div>
            </div>
        {% endif %}
    </div>
    <br>
</div>
{% endblock %}